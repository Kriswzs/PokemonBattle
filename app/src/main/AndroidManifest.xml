<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kriswang.mp6">

    <uses-permission android:name="android.permission.INTERNET"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".p001" />
        <activity android:name=".p002" />
        <activity android:name=".mp001" />
        <activity android:name=".mp002" />
        <activity android:name=".mp003" />
        <activity android:name=".mp004" />
        <activity android:name=".mp005" />
        <activity android:name=".np001" />
        <activity android:name=".np002" />
        <activity android:name=".np003"></activity>
    </application>

</manifest>